#include<stdio.h>
int btd(int b[],size){
int d,j;
for(j=0;j<=size;j++){
b[j]*
}
}
int main(){
    int bin[];
    int dec,n,c=0;
    printf("Enter the no of digits in binary number:");
    scanf("%d",&n);
    printf("Enter the binary number whose equivalent decimal is to be calculated:");
    for(i=0;i<=n;i++){
        int dig;
            printf("Enter the %d th digit:");
     bin[i]=scanf("%d",&dig);
    c++;
    }
    dec=btd(bin[],c);
    printf("The decimal equivalent is:%d",dec);
return 0;
}
