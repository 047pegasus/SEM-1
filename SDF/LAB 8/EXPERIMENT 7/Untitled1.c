#include<stdio.h>
void main(){
    int num,rem,sum=0;
    printf("Enter a three digit number:");
    scanf("%d",&num);
    while(n!=0){
        rem=num/10;
        sum=sum+rem;
    }
    printf("The sum of the three digits is:");
    printf("%d",sum);
return 0;
}
